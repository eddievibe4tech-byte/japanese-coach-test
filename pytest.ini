[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --cov=src
    --cov-report=term-missing
    --cov-report=html:htmlcov
markers =
    unit: 單元測試
    integration: 整合測試
    slow: 慢速測試
    api: 需要外部 API 的測試
